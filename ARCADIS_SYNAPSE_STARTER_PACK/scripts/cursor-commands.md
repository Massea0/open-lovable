# 🎯 CURSOR COMMANDS CHEAT SHEET

## 🚀 Commandes Essentielles pour le Fork VSCode

### Phase 1 : Setup Initial

```bash
# 1. Configuration du projet
Cmd+K: "Setup this VSCode fork for development with all necessary build tools and dependencies"

# 2. Nettoyage Microsoft
Cmd+K: "Remove all Microsoft branding, telemetry, and tracking from this VSCode fork"

# 3. Rebranding Arcadis
Cmd+K: "Rebrand this VSCode fork to Arcadis Synapse with blue theme (#0054A6)"
```

### Phase 2 : Extension Development

```bash
# 1. Créer l'extension de base
Cmd+K: "Create a VSCode extension with activity bar icon, webview panel, and WebSocket client"

# 2. Ajouter le chat UI
Cmd+K: "Create a beautiful chat interface like ChatGPT in this webview with markdown support"

# 3. Implémenter les commandes
Cmd+K: "Add commands for Cortex (architect) and Neuron (engineer) agents to this extension"
```

### Phase 3 : Dual-Agent Architecture

```bash
# 1. Service Orchestrateur
Cmd+K: "Create a WebSocket server that routes messages between VSCode and AI agents"

# 2. Agent Cortex
Cmd+K: "Implement Cortex agent that analyzes code and creates execution plans"

# 3. Agent Neuron
Cmd+K: "Implement Neuron agent that generates and executes code based on Cortex instructions"
```

### Phase 4 : Features Avancées

```bash
# 1. Inline Chat (Cmd+K)
Cmd+K: "Add inline chat feature like Cursor's Cmd+K to this editor"

# 2. Auto-completion IA
Cmd+K: "Implement AI-powered code completion similar to GitHub Copilot"

# 3. Context Awareness
Cmd+K: "Add workspace indexing with vector embeddings for context-aware suggestions"
```

---

## 💡 Prompts Optimisés par Contexte

### Pour le Debugging

```bash
# Erreur spécifique
Cmd+K: "Debug this error: [paste error]. Check for common VSCode extension issues"

# Performance
Cmd+K: "Profile this code and optimize for production with caching"

# Memory leaks
Cmd+K: "Find and fix memory leaks in this VSCode extension"
```

### Pour l'UI/UX

```bash
# Animations
Cmd+K: "Add smooth animations and transitions to this React component"

# Responsive design
Cmd+K: "Make this UI responsive and accessible (WCAG 2.1 AA)"

# Dark mode
Cmd+K: "Implement proper dark/light theme switching"
```

### Pour l'Architecture

```bash
# Patterns
Cmd+K: "Refactor this code using best practices and design patterns"

# Tests
Cmd+K: "Generate comprehensive unit tests with 90% coverage"

# Documentation
Cmd+K: "Add JSDoc comments and generate API documentation"
```

---

## 🔥 Composer Commands (Cmd+Shift+I)

### Refactoring Majeur

```markdown
Task: Refactor this VSCode extension for production

Requirements:
1. Implement proper error handling
2. Add comprehensive logging
3. Optimize performance
4. Add retry logic
5. Implement graceful degradation
6. Add progress indicators
7. Cache responses
8. Handle edge cases
```

### Architecture Complète

```markdown
Task: Implement complete dual-agent architecture

Components:
1. Cortex Agent - Architectural intelligence
2. Neuron Agent - Engineering execution
3. Orchestrator - Coordination service
4. Context Manager - Workspace awareness
5. Knowledge Base - Learning system
6. Metrics Collector - Performance tracking

Use TypeScript, WebSockets, and follow VSCode extension best practices.
```

---

## 🎨 UI Generation Commands

### Chat Interface

```bash
Cmd+K: "Create a chat interface with:
- Message bubbles with avatars
- Markdown rendering
- Code syntax highlighting
- Copy button for code blocks
- Streaming responses
- Typing indicators
- Error states
- Loading skeletons"
```

### Settings Page

```bash
Cmd+K: "Create a settings page with:
- Model selection dropdowns
- API key inputs (masked)
- Temperature sliders
- Theme toggles
- Keybinding configuration
- Import/export settings"
```

---

## 🛠️ Build & Deploy Commands

### Build Optimization

```bash
Cmd+K: "Optimize this VSCode build:
- Reduce bundle size
- Enable tree shaking
- Minimize dependencies
- Optimize images
- Enable compression"
```

### Distribution

```bash
Cmd+K: "Create distribution scripts for:
- macOS (DMG with code signing)
- Windows (MSI installer)
- Linux (AppImage, DEB, RPM)
- Auto-updater integration"
```

---

## 🐛 Troubleshooting Commands

### Common Issues

```bash
# Extension not loading
Cmd+K: "Debug why this VSCode extension is not loading. Check manifest and activation events"

# WebSocket connection fails
Cmd+K: "Fix WebSocket connection issues between extension and service"

# Build fails
Cmd+K: "Diagnose and fix VSCode build errors. Check node-gyp and native dependencies"
```

---

## 📝 Documentation Commands

```bash
# README
Cmd+K: "Create a professional README with badges, installation, usage, and contribution guide"

# API Docs
Cmd+K: "Generate API documentation from TypeScript interfaces and JSDoc comments"

# User Guide
Cmd+K: "Create a user guide with screenshots and GIFs for main features"
```

---

## 🚀 Performance Commands

```bash
# Startup optimization
Cmd+K: "Optimize VSCode startup time. Lazy load extensions and defer non-critical operations"

# Memory optimization
Cmd+K: "Reduce memory usage. Implement disposal patterns and cleanup listeners"

# Rendering optimization
Cmd+K: "Optimize React rendering with memo, useMemo, and useCallback"
```

---

## 💎 Pro Tips

### 1. Context is King
Always provide context about:
- Current file type
- Project structure
- Error messages
- Expected behavior

### 2. Iterative Refinement
```bash
# First pass
Cmd+K: "Create basic implementation"

# Second pass
Cmd+K: "Add error handling and validation"

# Third pass
Cmd+K: "Optimize for performance and add tests"
```

### 3. Reference Files
```bash
# Use @-mentions
Cmd+K: "Refactor this following patterns in @src/patterns.ts"

# Reference documentation
Cmd+K: "Implement this according to @docs/architecture.md"
```

### 4. Batch Operations
```bash
# Multiple files at once
Composer: "Update all files in this directory to use the new API"
```

---

## 🎯 Quick Wins

```bash
# Instant improvements
Cmd+K: "Add TypeScript types to this JavaScript file"
Cmd+K: "Convert callbacks to async/await"
Cmd+K: "Add input validation and sanitization"
Cmd+K: "Implement proper error boundaries"
Cmd+K: "Add loading and error states"
```

---

*Remember: Cursor is your pair programmer. Be specific, provide context, and iterate quickly!*